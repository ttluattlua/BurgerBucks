<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<!-- CREATE TABLE ADDR(
	SEQ NUMBER(10) PRIMARY KEY,
	MEMBER_SEQ NUMBER(10) NOT NULL,
	ADDRESS VARCHAR2(100) NOT NULL,
	LAT NUMBER(10) NOT NULL,
	LNG NUMBER(10) NOT NULL,
	MEMO VARCHAR2(200),
	DEL NUMBER(2) NOT NULL
); -->

<mapper namespace="KHAddress">

	<select id="allAddress" 
		resultType="kh.com.a.model.Bb_AddrDto"> 
		SELECT SEQ, MEMBER_SEQ, ADDRESS, LAT, LNG, MEMO, DEL 
		FROM ADDR WHERE SEQ=1 
	</select> <!-- 쿼리오케이  -->

</mapper>  
